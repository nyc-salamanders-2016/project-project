<% if logged_in? %>
	<% if admin? %>
		<%= react_component 'AdminView', pitches: @pitches, admin: current_user.admin?, teams: @teams %>	
	<% else %>
		<%= react_component 'StudentView', pitches: @pitches, admin: current_user.admin? %>	
	<% end %>
<% else %>
	<h3>You do not have access to this page 😀</h3>	
<% end %>
